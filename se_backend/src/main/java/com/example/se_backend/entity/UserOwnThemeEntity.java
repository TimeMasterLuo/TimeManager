package com.example.se_backend.entity;

import javax.persistence.Entity;
import javax.persistence.Table;

@Entity
@Table(name = "user_own_theme", schema = "SE-DB", catalog = "")
public class UserOwnThemeEntity {
}
